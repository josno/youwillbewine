import React, { useState, useEffect } from "react";
import styled from "styled-components";
import Button from "../Components/Button";

const Pair = (props) => {
	return <PairStyles></PairStyles>;
};

const PairStyles = styled.div``;

export default Pair;
